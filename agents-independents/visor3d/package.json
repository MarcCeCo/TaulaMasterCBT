{
  "name": "taulamaster-visor3d",
  "version": "1.0.0",
  "description": "Agent Visor 3D — sync ACC → Supabase",
  "main": "dist/src/server.js",
  "scripts": {
    "build": "cd .. && npm install && cd visor3d && ../node_modules/.bin/tsc",
    "start": "node dist/src/server.js",
    "dev": "../node_modules/.bin/ts-node src/server.ts"
  }
}
