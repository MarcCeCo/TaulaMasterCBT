{
  "name": "taulamaster-token-service",
  "version": "1.0.0",
  "description": "Token APS 3-legged per al Viewer SDK",
  "main": "dist/src/server.js",
  "scripts": {
    "build": "cd .. && npm install && cd token-service && ../node_modules/.bin/tsc",
    "start": "node dist/src/server.js",
    "dev": "../node_modules/.bin/ts-node src/server.ts"
  }
}
